# Notes Frontend

A simple local-first notes app (React + Vite) with CRUD, search, tag filtering, and persistence via `localStorage`.

## Run locally

```bash
npm install
npm run dev
```

The app runs on port `REACT_APP_PORT` (default in `.env` is `3000`).
